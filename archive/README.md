# Archive

This directory contains historical academic starter code and earlier/simple experiments kept for provenance.

The maintained public implementation lives in `../src`:

- `src/shared`
- `src/baseline`
- `src/attention`

Use the commands documented in the root `README.md` for training and inference.
